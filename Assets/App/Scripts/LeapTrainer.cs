using UnityEngine;
using System.Collections;

public class LeapTrainer : MonoBehaviour {
	/*
	 * It will be done using the RecordingControls class and extract the alghoritm from leaptrainer.js or some of alghoritms from papers
	 */
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
